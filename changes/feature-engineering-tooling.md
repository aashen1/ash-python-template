Add typed configuration management via `pydantic-settings`, structured
logging via `structlog`, an architecture decision records (ADR) directory,
conventional commit enforcement in pre-commit, expanded Ruff complexity
rules (`C90`, `PL`), and example tests demonstrating `pytest-mock` and
`responses`.
